<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @bip_configs.each do |bip_config| %>
  <tr>
    <td><%= bip_config.name %></td>
    <td><%= link_to 'Show', bip_config %></td>
    <td><%= link_to 'Destroy', bip_config, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />