var newDiv = $(document.createElement('div'));
newDiv.html('<%= escape_javascript(render(:partial => @bipnode)) %>'); 
newDiv.dialog({ title: 'Node' , modal: true, width: 'auto', heigh: 'auto' }).draggable();