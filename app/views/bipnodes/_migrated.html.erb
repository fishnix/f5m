<%= div_for(bip_node, :class => "bip_migration_status") do %>
	<% if bip_node.migrated %>
		<div class="bip_object_migrated">
		Migrated.<br />
		<%= link_to "Not Migrated?, (Click Here)", bip_config_bipnode_unmigrate_path(@bip_config.id, bip_node.id), 
									:method => :put, :remote => true %>
		</div>
	<% else %>
		<%= link_to "Mark Migrated", bip_config_bipnode_migrate_path(@bip_config.id, bip_node.id), 
								:method => :put, :remote => true %>
	<% end %>
<% end %>