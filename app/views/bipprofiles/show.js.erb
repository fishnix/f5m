var newDiv = $(document.createElement('div'));
newDiv.html('<%= escape_javascript(render(:partial => @bipprofile)) %>'); 
newDiv.dialog({ title: 'Profiles' , modal: true, width: 'auto', heigh: 'auto' }).draggable();