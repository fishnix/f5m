<%= div_for(bip_pool, :class => "bip_migration_status") do %>
	<%= bip_pool.migrated %>
	<% if bip_pool.migrated %>
		<%= form_for [@bip_config, bip_pool], :remote => true do |f| %>
			<%= f.hidden_field :migrated, :value => false %>
			<%= f.submit "Not Migrated!" %>
		<% end %>
	<% else %>
		<%= form_for [@bip_config, bip_pool], :remote => true do |f| %>
			<%= f.hidden_field :migrated, :value => true %>
			<%= f.submit "Migrated!" %>
		<% end %>
	<% end %>
<% end %>