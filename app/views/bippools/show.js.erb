var newDiv = $(document.createElement('div'));
newDiv.html('<%= escape_javascript(render(:partial => @bippool)) %>'); 
newDiv.dialog({ title: 'Pool' , modal: true, width: 'auto', heigh: 'auto' }).draggable();