<%= div_for(bip_virtual, :class => "bip_migration_status") do %>
	<%= bip_virtual.migrated %>
	<% if bip_virtual.migrated %>
		<%= form_for [@bip_config, bip_virtual], :remote => true do |f| %>
			<%= f.hidden_field :migrated, :value => false %>
			<%= f.submit "Not Migrated!" %>
		<% end %>
	<% else %>
		<%= form_for [@bip_config, bip_virtual], :remote => true do |f| %>
			<%= f.hidden_field :migrated, :value => true %>
			<%= f.submit "Migrated!" %>
		<% end %>
	<% end %>
<% end %>