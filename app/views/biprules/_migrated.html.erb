<%= div_for(bip_rule, :class => "bip_migration_status") do %>
	<% if bip_rule.migrated %>
		<div class="bip_object_migrated">
		Migrated.<br />
		<%= link_to "Not Migrated?, (Click Here)", bip_config_biprule_unmigrate_path(@bip_config.id, bip_rule.id), 
									:method => :put, :remote => true %>
		</div>
	<% else %>
		<%= link_to "Mark Migrated", bip_config_biprule_migrate_path(@bip_config.id, bip_rule.id), 
								:method => :put, :remote => true %>
	<% end %>
<% end %>