<%= div_for(bip_monitor, :class => "bip_migration_status") do %>
	<% if bip_monitor.migrated %>
		<div class="bip_object_migrated">
		Migrated.<br />
		<%= link_to "Not Migrated?, (Click Here)", bip_config_bipmonitor_unmigrate_path(@bip_config.id, bip_monitor.id), 
									:method => :put, :remote => true %>
		</div>
	<% else %>
		<%= link_to "Mark Migrated", bip_config_bipmonitor_migrate_path(@bip_config.id, bip_monitor.id), 
								:method => :put, :remote => true %>
	<% end %>
<% end %>